{
  "name": "my-blog",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:ssr": "node scripts/dev-ssr.mjs",
    "build": "vite build",
    "build:client": "vite build --outDir dist/client --ssrManifest",
    "build:server": "vite build --ssr src/entry-server.ts --outDir dist/server",
    "build:ssr": "npm run build:client && npm run build:server && node scripts/prerender.mjs",
    "preview": "vite preview",
    "preview:ssr": "npm run build:ssr && node scripts/run-server.mjs production",
    "preview:ssr:local": "npm run build:ssr && node scripts/run-server.mjs preview-local",
    "preview:pages": "npm run build:ssr && node scripts/run-pages-dev.mjs production",
    "preview:pages:local": "npm run build:ssr && node scripts/run-pages-dev.mjs local",
    "type-check": "vue-tsc --noEmit -p tsconfig.json",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "@fancyapps/ui": "^6.0.32",
    "@tailwindcss/vite": "^4.1.8",
    "axios": "^1.10.0",
    "tailwindcss": "^4.1.8",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "vue3-lazyload": "^0.3.8"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "@vitejs/plugin-vue": "^5.2.3",
    "prettier": "3.5.3",
    "typescript": "^5.8.3",
    "vite": "^6.2.4",
    "vite-plugin-vue-devtools": "^7.7.2",
    "vue-tsc": "^2.2.10"
  }
}