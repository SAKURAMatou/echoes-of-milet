# Vite 下 Vue 的环境变量命名规则和使用方式
#
# 1. 命名规则:
#    - 所有环境变量必须以 `VITE_` 前缀开头，例如：VITE_API_URL。
#    - 只有以 `VITE_` 开头的变量才会暴露给客户端代码。
#
# 2. 定义方式:
#    - 在项目根目录下创建 `.env` 文件（如 `.env`, `.env.development`, `.env.production`）。
#    - 在文件中以 `KEY=VALUE` 形式定义变量，例如：VITE_API_URL=https://api.example.com
#
# 3. 使用方式:
#    - 在 Vue 组件或 JS/TS 文件中通过 `import.meta.env` 访问，例如：import.meta.env.VITE_API_URL
#
# 4. 注意事项:
#    - 修改环境变量后需重启开发服务器。
# 5. 其他说明:
#    - 如果你想把一个分支的改动合并到另一个分支，可以使用以下 git 命令：
#      1. 切换到目标分支（你想合并到的分支）：git checkout 目标分支名
#      2. 执行合并命令：git merge 源分支名
#      3. 如果有冲突，解决冲突后再提交：git add . && git commit
